---
# Copy this file, rename to a-slug.md (no leading underscore), set draft: false.
# Entries are private behind the gate in src/data/dream-journal-gate.ts.

date: 2026-01-15
draft: true

# Optional. Shown under the date.
mood: weightless, curious

# Optional comic-style emphasis. `highlight-words` get the default amber treatment;
# `word-styles` overrides individual words with amber | violet | burst.
highlight-words:
  - drifted
  - ceiling
word-styles:
  door: burst
  light: violet
---

The room *tilted* without moving. I drifted toward a door that was only a seam in the ceiling, and behind it a thin light hummed like a held note.

When I touched the frame, the sound folded into color — not loud, just *certain*, the way a comic panel makes one word take up the whole row.
